# Tasks: Improve Toggle Button Visibility

## Implementation Tasks

- [x] Update `.toggle-btn` base style in `viewer_v2/css/styles.css` to use `background-color: white` instead of `transparent`
- [x] Verify active state still displays correctly with blue background
- [x] Test hover state still provides visual feedback
- [x] Manually verify all three buttons (Parsed, Raw, Notes) render correctly in both states
