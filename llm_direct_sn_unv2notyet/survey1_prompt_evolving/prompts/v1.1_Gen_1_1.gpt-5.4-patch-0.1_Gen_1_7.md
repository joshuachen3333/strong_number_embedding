- Place `H0834 (אֲשֶׁר)` exactly on the UNV-aligned relative marker, not on a convenient Chinese structural particle.
- Never attach `H0834` to `的`; treat `的` as a Chinese structural particle with no SN unless the source explicitly requires otherwise.
- When UNV realizes `אֲשֶׁר` through `將`, tag `將` with `H0834`.
- When UNV realizes `אֲשֶׁר` as an implied element in braces, preserve the braces and place `{<WH0834>}` exactly at that position.
- For `H0834`, prioritize exact positional matching to the UNV source over surface-level Chinese grammar intuition.
- Keep aspect particles such as `了` attached to the full verb block; place SN tags after the complete written word, not between the verb stem and `了`.
- Before finalizing, explicitly check two failure modes: `H0834` wrongly on `的`, and SN tags inserted before a final `了`.