{'best': '**SN Placement Rules — Grouping & Attachment**\n\n1. **Unified phrase → grouped tags.** When a Chinese phrase translates multiple Hebrew words as a single conceptual unit (e.g., copula + predicate like "還沒有定形" for היה + תהו), place all corresponding SN tags together at the end of the entire phrase. Never split tags across fragments of the phrase.\n\n2. **Preposition/supplied-word tags attach immediately.** When a supplied word (from braces like `{<WAH05921>}`) corresponds to a Chinese preposition or relational word (e.g., 在, 從, 向), attach the SN tag directly after that word — not after a later word in the prepositional phrase. Example: `在<WAH05921>深淵` not `在深淵...<WAH05921>`.\n\n3. **Split-frame prepositions (在...上面, 從...中).** Chinese often wraps a noun with a preposition frame. The preposition SN attaches to the first element (在), the noun SN attaches to the noun (深淵), and the positional word (上面) carries its own SN if applicable. Do not cluster all tags at the frame\'s end.\n\n4. **Copula + predicate pairs stay together unless Chinese separates them.** If Chinese merges a Hebrew copula (היה) and its complement into one phrase, keep their SNs together. Only split them if the Chinese text itself clearly uses separate clauses or words for each.\n\n5. **One stable output per attempt.** Do not produce duplicate hypotheses. If your current output matches a previous attempt exactly, declare convergence rather than emitting it again as a new variant.', 'confidence': 0.3, 'notes': ['JSON parse failed, raw output used']}