# Patch 0.1 for opus
# Triggered by: Gen 2:10
# Date: 2026-06-25 09:19
# Base prompt: v1.2
# Stable models: agy, codex
# Instability: moderate (score=2, unique_outputs=4)
#
# --- Convergence story ---
# opus failed to converge easily (stable_at=R2c, 6 attempts)
# while agy, codex agreed on the same output.
#
# opus's attempts:
#   R1: 有河<05104>從<04480>伊甸<05731>流出來<03318><8802>、灌漑<09001><08248><8687><0853>那園子<01588>，從<04480>那裏<08033>分支<06504><8735>、成<019...
#   R2a: 有河<05104>從伊甸<04480><05731>流出來<03318><8802>、灌漑<09001><08248><8687><0853>那園子<01588>，從那裏<04480><08033>分支<06504><8735>、成<019...
#   R2b: 有河<05104>從<04480>伊甸<05731>流出來<03318><8802>、灌漑<09001><08248><8687><0853>那園子<01588>，從<04480>那裏<08033>分支<06504><8735>、成<019...
#   R2c: 有河<05104>從<04480>伊甸<05731>流出來<03318><8802>、灌漑<09001><08248><8687><0853>那園子<01588>，從<04480>那裏<08033>分支<06504><8735>、成了<01...
#   R2d: 有河<05104>從<04480>伊甸<05731>流出來<03318><8802>、灌漑<09001><08248><8687><0853>那園子<01588>，從<04480>那裏<08033>分支<06504><8735>、成<019...
#   R2e: 有河<05104>從<04480>伊甸<05731>流出來<03318><8802>、灌漑<09001><08248><8687><0853>那園子<01588>，從<04480>那裏<08033>分支<06504><8735>、成了<01...
#
# --- Feedback from stable models ---
# [agy] feedback:
#   {
#     "mistakes": "The model improperly separates stacked SNs that were grouped at the end of prepositional phrases in the U
#     "oscillation_locations": "The model oscillates on the position of <04480> (whether it follows '從' or is stacked after 
#     "oscillation_causes": "The instability stems from a conflict between the UNV source's convention of grouping and stack
#     "actionable_feedback": "Strictly replicate the SN grouping and stacking patterns found in the UNV reference. If the UN
#   ... (1507 chars total)
# [codex] feedback:
#   The model’s main problem is not missing SNs; it is unstable phrase-boundary placement.
#   
#   **1. Specific SN Placement Mistakes**
#   
#   - It repeatedly splits bound source phrases that should stay grouped:
#   ... (3192 chars total)
#
# --- Self-patch (written by opus) ---

# ROOT CAUSE ANALYSIS

## Why I produced 4 different outputs

I oscillated because I never committed to a **single alignment philosophy**, and I re-segmented the Chinese text on every pass. Two incompatible strategies were both active in my head, and I silently switched between them:

1. **Hebrew-morpheme alignment** — "`<04480>` means *from*, so it goes on `從`; `<05731>` is *Eden*, so it goes on `伊甸`." This pulls the stacked tags apart into `從<04480>伊甸<05731>`.
2. **UNV source-group preservation** — "The UNV reference shows `從伊甸<04480><05731>` as one stacked block, so reproduce that block." This keeps `從伊甸<04480><05731>`.

Both feel locally correct, so each new pass I'd "re-derive" the placement from scratch and sometimes land on strategy 1, sometimes strategy 2. Nothing froze the decision, so there was nothing to be consistent *with*.

## The three specific oscillation points and their hidden ambiguity

- **`<04480><05731>` (從伊甸)** and **`<04480><08033>` (從那裏)** — Ambiguity: Hebrew מִן ("from") is a prefix bound to its noun, but I treated `從` as a standalone Chinese word deserving its own tag. The UNV reference already resolved this by stacking both tags at the *end* of the whole prepositional phrase. I ignored the reference's grouping and re-litigated it morpheme-by-morpheme.
- **`<01961><8804>` (成了)** — Ambiguity: is the aspect particle `了` part of the verb, or a separate untagged word that should sit *outside* the tag? I couldn't decide whether the tag attaches after `成` or after `成了`, so `了` drifted across the tag boundary between passes (`成<01961><8804>了` ↔ `成了<01961><8804>`).
- **Punctuation (`：` vs `。`)** — I was copying surface tokens from the UNV source / my own prior attempts instead of locking the target wording first. Because the phrase boundaries were never frozen, even punctuation became "live" and mutable.

**The single deepest cause:** I did not separate the task into two ordered steps. I tried to decide *Chinese phrase boundaries* and *SN attachment* simultaneously, on every pass. When boundaries are still fluid, attachment can't stabilize — so it never converged.

---

# COMPLETE INSTRUCTIONS FOR SN PLACEMENT

These replace all prior placement instructions. Follow them exactly and in order.

## Core principle

**Strong's Numbers are transferred at the PHRASE/GROUP level by replicating the UNV source grouping — NOT re-derived word-by-word from Hebrew morphology.** The UNV+SN reference has already decided how tags group and stack. Your job is to carry those *same groups* onto the target wording, never to re-segment them.

## The two-phase rule (this is what prevents oscillation)

You MUST do these in order. Never let phase 2 reach back and modify phase 1.

### Phase 1 — Freeze the target wording and its phrase boundaries

1. Write out the final target-translation wording **once**, including final punctuation. Copy punctuation from the **target text**, never from the UNV source or from a previous attempt.
2. Segment that wording into translation units (phrase chunks). A "unit" is a chunk that corresponds to one stacked SN group in the source. Examples from this verse: `有河` / `從伊甸` / `流出來` / `灌漑` / `那園子` / `從那裏` / `分支` / `成了` / `四` / `道`.
3. **Once written, these boundaries and this punctuation are LOCKED.** On any later pass, do not re-segment `從伊甸`, `從那裏`, or `成了`, and do not change punctuation. If you feel the urge to re-split a chunk, that urge is the oscillation bug — suppress it.

### Phase 2 — Attach SN groups to the frozen units

1. For each UNV source group, take the **entire stacked block of tags** and attach it to the **end** of the corresponding frozen target unit.
2. Never distribute the tags of one stacked block across multiple characters. A stacked source block stays a stacked block.

## Explicit decision rules for the ambiguous cases

These are not judgment calls. Apply them mechanically.

### Rule A — Preposition + noun/adverb stays grouped, tags stack at the END of the phrase

When the UNV source stacks `<from-tag><noun-tag>` together (Hebrew מִן and its object), reproduce that exact stacked block at the end of the whole Chinese prepositional phrase. Do **not** peel the "from" tag off onto the Chinese preposition character.

- ✅ `從伊甸<04480><05731>`  ❌ `從<04480>伊甸<05731>`
- ✅ `從那裏<04480><08033>`  ❌ `從<04480>那裏<08033>`

General form: `從+NOUN<from-tag><noun-tag>`, never `從<from-tag>NOUN<noun-tag>`.

### Rule B — Aspect/result particles (了, 著, 過, 起來, 出來) belong WITH their verb, inside the SN unit

A Chinese verb compound formed with an aspect/result particle is ONE translation unit. The SN attaches after the **whole compound**, not between the verb stem and its particle. The particle is never left dangling outside the tag.

- ✅ `成了<01961><8804>`  ❌ `成<01961><8804>了`
- The following numeral phrase is independent: `四<09001><0702>` and `道<07218>` keep their own tags. Never let `<09001><0702>` slide onto `了四` — `了` belongs to `成了`, `四` carries `<09001><0702>`.

### Rule C — Punctuation is copied from the TARGET text and frozen

Use the target translation's own ending punctuation. Do not import `：` or any mark from the UNV source, and do not change it across passes. If passes disagree on punctuation, you violated Phase 1 — punctuation should have been locked and never reconsidered.

### Rule D — Conflict resolver (the tie-breaker that ends oscillation)

If "Hebrew morpheme alignment" and "UNV source grouping" disagree about where a tag goes, **UNV source grouping ALWAYS wins.** There is no case in this task where morpheme-by-morpheme alignment overrides the reference grouping. Stop re-deriving from Hebrew; replicate the reference.

## Self-check before emitting (run once, then STOP)

1. Does every stacked source block appear as an intact block on exactly one frozen target unit? (No split blocks.)
2. Are all `從+NOUN` phrases grouped per Rule A? (No tag on a bare `從`.)
3. Is every aspect particle inside its verb's SN unit per Rule B? (No `了` outside the tag.)
4. Does ending punctuation match the target text per Rule C?
5. Is the set of SNs identical to the source (none lost, none invented, order preserved)?

If all five pass, **emit immediately and do not produce another variant.** Re-opening a passed output is the oscillation failure mode. One correct, frozen answer — not four re-derivations.

## Reference alignment for this verse (the locked target)

```text
有河      = <05104>
從伊甸    = <04480><05731>
流出來    = <03318><8802>
灌漑      = <09001><08248><8687><0853>
那園子    = <01588>
從那裏    = <04480><08033>
分支      = <06504><8735>
成了      = <01961><8804>
四        = <09001><0702>
道        = <07218>
```