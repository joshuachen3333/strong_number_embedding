# 實際 prompt 還原

## Survey8 概念誕生 — 用戶的完整描述
用戶: "survey4_self_supervised_prompt_tuning_simplestSN 是這樣, 大多比照 survey4_self_supervised_prompt_tuning. 但是我們的標準答案先經過一個去殼的動作,
1, 要分新約與舊約.
2, unv+sn 先經過 去殼動作, 得到 unv+(simplestSN) 作為標準答案.
3, 26 dim 的 unv+sn 要輸入 llm 之前也比照這種去殼處理,
4, output 的 unv+sn 也是直接以 simplest 的僅有數字型態輸出.
5, python 再經過原來的加殼邏輯 (survey2 有各種 append0 各種括號, 各種英文前綴) 把可能對應 groud true 複雜加殼的型態套加上去

這樣重點就是, llm (sonnet, opus, deepseek) 僅處理最單純的 SN 號碼加插, 真的就是號碼. 把複雜的各種外套括號, 前綴英文字交給 script
這樣可行嗎？
甚至 script 還能補微調對調這種 9xxx 8xxx 位置是否恰當需要三度更改位置的後處理 (如果有明確程式邏輯可依循)

這種 _simplestSN 會讓 survey5, survey6 一起參考甚至共用, 例如蛻殼的動作, 加回殼或者對調調整位置時的共同後處理"

## 去殼的 braces 也精省
用戶: "{<WH0853>} → {853} 為什麼會這樣啊？能否把 {853} 的 {} 都精省去? 等套殼時再想到加回來?"

## 字典提供 SN 號碼
用戶: "或者一不做二不休, 乾脆把 類似 希伯來原文, SN:word (simplestSN) 一起給, 這樣模型雖然面對不同 example library 經節, 但是有原文經文與 SN:原文字 無可推諉"
